// This is file with demos of your component
// Each export is one usecase for your component

// Note: Replaced absolute alias import '@/components/ui/image-auto-slider' with relative './image-auto-slider' as there is no path alias setup
import { Component } from "./image-auto-slider";

const DemoOne = () => {
  return <Component />;
};

export { DemoOne };
